# Place this file in htdocs/ (same level as app/, bootstrap/, public/).
# InfinityFree cannot change the document root; this forwards requests to Laravel's public/ folder.

<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
