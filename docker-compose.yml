services:
  mysql:
    image: mysql:8.0
    container_name: examguard-mysql
    restart: unless-stopped
    ports:
      - "3306:3306"
    environment:
      MYSQL_DATABASE: examguard
      MYSQL_ROOT_PASSWORD: root
    volumes:
      - examguard_mysql:/var/lib/mysql

volumes:
  examguard_mysql:
